import React from 'react'
import ContainerSection from './styled.js'

const Container = ({children}) => {
  return (
    <ContainerSection>{children}</ContainerSection>
  )
}

export default Container
